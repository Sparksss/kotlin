CREATE TABLE Employees(
                       id integer PRIMARY KEY,
                       first VARCHAR(100),
                       last VARCHAR(100),
                       age integer
                       );

CREATE TABLE Persons(
                    id integer PRIMARY KEY,
                    name VARCHAR(100)
                    );